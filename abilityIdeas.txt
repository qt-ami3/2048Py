added:
  bomb:
    blows up a node.
  freeze:
    freezes a node for a turn.

pipe:
  a directional pipe that allows occupying nodes to traverse only one axis.

function block:
  allows use of python for scripting.
